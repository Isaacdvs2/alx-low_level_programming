#include <stdio.h>
#include "main.h"

/**
 * add - method to add two integers
 * @a: the first int to add.
 * @b: the second int.
 *
 * Description: a prog that add two ints together.
 * Return: return the result
 */

int add(int a, int b)
{
	int result;

	result = a + b;
return (result);
}
