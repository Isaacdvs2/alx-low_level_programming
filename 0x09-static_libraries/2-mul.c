#include <stdio.h>
#include "main.h"

/**
 * mul - a method to multiply two nums.
 * @a: the first num to multiply.
 * @b: the second num to multiply.
 *
 * Description: a prog that returns the
 * result of muliplying two nums.
 * Return: the product.
 */

int mul(int a, int b)
{
	return (a * b);
}
