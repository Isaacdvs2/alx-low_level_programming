#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
    char *str2;

    str = "0123456789";
    str2 = "melodies are the greatest things you'll ever hear   ";
    puts2(str);
    puts2(str2);
    return (0);
}
